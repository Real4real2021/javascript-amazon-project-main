import { deliveryOption } from "./data/deliveryOptons";

//ternary operator
const string = deliveryOption.deliveryCents 
=== 0   //if the first part returns true, the value is whatever follows the "?", if false, after the ":".
    ?   //if true
    :   //if false
//similar to an if statement, but the result can be saved in a variable.